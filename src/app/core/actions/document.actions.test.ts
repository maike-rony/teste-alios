import * as actions from './document.actions';

describe('Document Actions', () => {
  it ('should create DocumentRequest', () => {
    const action = actions.DocumentRequest({params: {}});
    expect(action.type).toEqual('[Document] Request');
  });

  it ('should create DocumentRequest', () => {
    const action = actions.DocumentRequest({params: {}});
    expect(action.type).toEqual('[Document] Request');
  });

  it ('should create DocumentRequest', () => {
    const action = actions.DocumentRequest({params: {}});
    expect(action.type).toEqual('[Document] Request');
  });
})
